<!DOCTYPE html>
<html lang="ko">
<head>
    <% include ../partials/head %>
</head>
<body>
	<!-- Navigation -->
	<% include ../partials/nav %>

	<!-- Introduction -->
	<% include ../partials/intro %>

	<!-- Content -->
	<div class="content">
		<div class="container">

      <!-- content start -->
      <div class="posts">
        <div class="show">


          <h3><%= post.title %></h3>

          <p>
            Author : <%= post.author.nickname %>
            &nbsp; | &nbsp;
            date :
            <%= post.createdAt.toLocaleDateString() %>
            <%= post.createdAt.toLocaleTimeString() %>
          </p>

          <hr />

          <div class="body board_desc">
            <%- escape(post.body).replace(/\n/g, '<br/>') %>
          </div>

          <hr />

          <div class="tool">
            <a href="/posts" class="btn">list</a>
            <% if( user && user.equals(post.author._id) ){ %>
            <a href="/posts/<%= post._id %>/edit"  class="btn">edit</a>
            <form class="btn_delete" action="/posts/<%= post._id %>?_method=delete" method="post" style="display:inline;">
              <button type="submit" name="button" class="btn" >delete</button>
            </form>
            <% } %>
          </div>
        </div>
      </div>
      <!-- //content start -->


		</div>
	</div>

	<footer>
		<% include ../partials/footer %>
	</footer>

	<!-- Javascript -->
	<% include ../partials/js %>
</body>
</html>
